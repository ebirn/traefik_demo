## traefik.yml

# Docker configuration backend
providers:
  docker:
    defaultRule: "Host(`{{ trimPrefix `/` .Name }}.docker.localhost`)"
  file:
    directory: /etc/traefik/dynamic
    watch: true

# API and dashboard configuration
api:
  insecure: true


metrics:
  prometheus: {}

pilot:
  token: "1bc51da9-fea7-42d6-a4b3-bae3168ef4c9"
